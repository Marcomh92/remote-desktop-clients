# DESIGN_PRINCIPLES.md

Cross-cutting rules and "facts" every change must respect. Each rule has a stable identifier so it can be referenced from code review, PR descriptions, and the rest of the docs (`PAT-*`, `PER-*`, `INV-*`, `DPP-*`, `ADR-*`).

> Read first: `MASTER.md`, `ARCHITECTURE.md`. The rules below are derived from the codebase, not invented for greenfield design.

---

## DPP — Design principles

| ID | Rule | Why |
|---|---|---|
| DPP-001 | **No Hilt, Koin, or Dagger.** Field injection and DI frameworks are not used. Wiring is explicit in the Activity. | Project shape (per AGENTS.md): Java-Views, no Compose, no annotation-processing DI. Adding one would balloon every flavor. |
| DPP-002 | **No Jetpack Compose.** UI is `android.view.View` and `androidx.appcompat.widget.*`. | Same rationale as DPP-001. Migrating eight wrappers off Views would be months of churn. |
| DPP-003 | **No `androidx.lifecycle.ViewModel`.** State lives on the Activity or in the protocol layer. | Same rationale. |
| DPP-004 | **No TypeScript-style DI Hilt-style `HiltViewModel` abstractions over RemoteConnection.** Use `RemoteConnection` plus its handler directly. | Project predates Jetpack. Adding it would not simplify the existing handwritten wiring. |
| DPP-005 | **Style is Java-first with a thin layer of Kotlin.** New utility/protocol code can be Kotlin, but Activities, Fragments, dialogs, custom Views stay Java. | Half-Kotlin/half-Java is the intentional state. Reverting either direction is out of scope. |
| DPP-006 | **The four flavors (VNC/RDP/SPICE/Opaque) share the canvas Activity.** No flavor-specific Activity subclasses; selection happens at runtime via `Utils.isRdp/isVnc/isSpice/isOpaque` checks. | Eight wrapper modules let us publish separate APKs without forking Activities. |
| DPP-007 | **Native libraries are loaded lazily and statically.** `LibFreeRDP` loads `libfreerdp-android.so` in its static initializer; `SpiceCommunicator` loads `libspice.so` + `libgstreamer_android.so`. No `System.loadLibrary` calls elsewhere. | Native search paths and side effects are centralized. |
| DPP-008 | **StrictMode is disabled on the canvas path.** `RemoteCanvasActivity.onCreate` calls `StrictMode.ThreadPolicy.permitAll()` because FreeRDP callbacks fire on native threads. | Without it, the bitmap-write path trips violations even though we synchronize carefully. Removing this requires a wider review of every cross-thread bitmap access. |
| DPP-009 | **Encrypted SQLite is the connection DB.** SQLCipher via `net.sqlcipher.database.SQLiteOpenHelper`. DB name `VncDatabase`. | Master-password protection is a documented feature. Plain SQLite breaks the master-password flow. |
| DPP-010 | **Per-flavor wrappers are manifest-only.** No `src/free/` source set; no `productFlavors` block. Paid/free is module choice. | Keeps each APK's build graph small and lets free flavor drop permissions at the manifest layer. |
| DPP-011 | **Pointer/keyboard language: "modifier" = `KeyEvent.META_*_ON` bit set.** "VK" = Windows Virtual Key (RDP wire), "scancode" = PC AT Set-1 scancode, "keysym" = X11 keysym (VNC), "Unicode" = UTF-16 code point. | Five terms — see PAT-002 for which codebase uses which. |
| DPP-012 | **All RDP keyboard and mouse output is funneled through one per-`RdpCommunicator` single-thread executor.** See INV-001. | Deterministic ordering, no interleaving from different UI threads. |
| DPP-013 | **The vendored FreeRDP core (`:remoteClientLib:...:freeRDPCore`) is upstream code.** Do not edit its C sources; treat the in-tree Java + prebuilt `.so` files as the contract. | Keeping it in-tree-vendored means we can patch the JNI surface and apply `gitnexus_query` reachability; touching the C side breaks the upgrade story. |

---

## INV — Invariants (things you must NOT break)

| ID | Invariant | Where documented |
|---|---|---|
| INV-001 | Every keyboard/mouse event for RDP is enqueued on `RdpCommunicator.inputExecutor`. Do not call `LibFreeRDP.sendKeyEvent` / `sendUnicodeKeyEvent` / `sendCursorEvent` from the UI thread or another worker. | `remoteClientLib/src/main/java/com/undatech/opaque/RdpCommunicator.java:62` |
| INV-002 | `RemoteCanvas` extends `AppCompatImageView` (not `SurfaceView`). The framebuffer is a `Bitmap` swapped via `setImageDrawable`. Do not replace this with a GL/Surface-backed renderer without revisiting every input coord-transform. | `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvas.java:62` |
| INV-003 | `UltraCompactBitmapData.updateBitmap(Bitmap, x, y, w, h)` is `synchronized (mbitmap)`. The same `Bitmap` instance is shared with the native renderer (`LibFreeRDP.updateGraphics`). Both sides observe the same memory object. | `bVNC/src/main/java/com/iiordanov/bVNC/UltraCompactBitmapData.java:82` |
| INV-004 | The native `Bitmap` reference is **owned by Java**. FreeRDP writes pixels into it via JNI, but does not own or recycle it. Do not call `Bitmap.recycle()` from native code. | `remoteClientLib/jni/libs/deps/FreeRDP/client/Android/Studio/freeRDPCore/src/main/java/com/freerdp/freerdpcore/services/LibFreeRDP.java` |
| INV-005 | `RemoteConnection.handler` is a **static** field. Two concurrent `RemoteCanvasActivity` instances race on it. See GAP-INV-005 below. | `bVNC/src/main/java/com/iiordanov/bVNC/protocol/RemoteConnection.java` |
| INV-006 | `RemoteCanvas.reDraw` rate-limits invalidate to ~60 Hz, falling back to 10 Hz when behind. Do not bypass this throttle to call `view.invalidate()` directly — the throttle and the `mbitmap` synchronization are paired. | `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvas.java:681` |
| INV-007 | `preferSendingUnicode` default is `true` (`preferSendingUnicodeDefaultValue` in `Constants.java`). When true, ASCII keys are sent via `processUnicodeKey` instead of `processVirtualKey`. | `bVNC/src/main/java/com/iiordanov/bVNC/Constants.java` |
| INV-008 | `RdpCommunicator.close()` sets `isInNormalProtocol=false` and starts `DisconnectThread`. It does NOT reset `RemoteKeyboardState.hardwareMetaState` or `RemoteKeyboardMetaState`. The RDP modifier row clears its own state via `RdpModifierRowHandler.resetRowState()` (called from `RemoteCanvasActivity.hideKeyboardAndExtraKeys`), which also calls `keyboard.clearMetaState()`. The legacy `clearMetaState()` site in `RemoteConnection.closeConnection` is retained. The hardware-meta leak across reconnects within the same Activity instance is **still not addressed** — that is the remaining gap. | `remoteClientLib/src/main/java/com/undatech/opaque/RdpCommunicator.java:204`, gap noted in §"Known gaps" below |
| INV-009 | `RdpKeyboardMapper.clearlAllModifiers()` exists publicly but is **not called from anywhere in the codebase**. It is the correct entry point for a modifier-reset refactor. | `remoteClientLib/src/main/java/com/undatech/opaque/input/RdpKeyboardMapper.java:664` (uncovered, by inspection) |
| INV-010 | The on-screen (sticky) modifier state is mirrored between the modifier UI widgets and `RemoteKeyboard.onScreenMetaState` exclusively via the canonical `clearMetaState` + `onScreen*Toggle` sequence. Two callers implement this bridge: legacy `ExtraKeysPagerAdapter.syncKeyboardModifierState(view)` and RDP `RdpModifierRowHandler.syncRowStateToKeyboard`. Clearing the panel without one of these leaves state drifted. | `bVNC/src/main/java/com/iiordanov/bVNC/extrakeys/ExtraKeysPagerAdapter.java:147`, `bVNC/src/main/java/com/iiordanov/bVNC/extrakeys/RdpModifierRowHandler.java:308` |
| INV-011 | Touch handlers generate `pointerMask` with a "down" bit (`POINTER_DOWN_MASK = 0x8000`). Each non-moving `sendPointerEvent` clears that bit and re-sends an "up" before the new "down", only if `prevPointerMask != 0`. This is the RDP wire-format guard that prevents a multi-button drag from confusing the remote. | `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpPointer.java:107` |
| INV-012 | `RemoteRdpPointer.scrollLeft` and `scrollRight` are **TODO stubs** — the RDP wire format here sends nothing. Horizontal scroll input events arrive at the handler and are silently dropped. | `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpPointer.java:73,79` |
| INV-013 | `RemoteKeyboardState.shouldSendModifier` suppresses duplicate modifier sends — once a hardware modifier is sent as a VK, the same modifier is never re-sent through `RdpCommunicator.sendModifierKeys` until state changes. This is paired with `RdpCommunicator.modifierMap` (`sendModifierKeys:222-234`). | `remoteClientLib/src/main/java/com/undatech/opaque/input/RemoteKeyboardState.java:194` |
| INV-014 | IME-committed text reaches RDP via `RemoteCanvas.onCreateInputConnection` returning a `BaseInputConnection`. The committed characters are split per-char inside `RemoteRdpKeyboard.processLocalKeyEvent` and forwarded as `KeyEvent(ACTION_MULTIPLE)` per char. | `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvas.java:823`, `RemoteRdpKeyboard.java:70` |
| INV-015 | `KEYCODE_MENU` is handled twice: `RemoteRdpKeyboard.processLocalKeyEvent` returns true (consumed) and `RemoteClientsInputListener.onKey` explicitly delegates back to `activity.onKeyDown/onKeyUp` for the Activity's normal menu handling. | `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteClientsInputListener.kt:52` |
| INV-016 | `RemoteRdpKeyboard.KeyDispatchedListener` fires only on `ACTION_DOWN` / `ACTION_MULTIPLE`, only for non-modifier keycodes, only after `RdpKeyboardMapper.processAndroidKeyEvent` (or the per-char IME path) returns. The ACTION_UP replay uses `lastDownMetaState` so listeners must not see it. Listeners must NOT clear LOCKED modifiers; only ON (non-locked) modifiers may be consumed. Round 6: `KEYCODE_TAB` is exempted from the listener (`:127-131`) so a Tab tap does not consume one-shot modifiers — Alt+Tab cycles instead of pressing-and-releasing LMENU. | `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpKeyboard.java:29-38, 118-135` |
| INV-017 | `ZoomScaling.computeMinimumScale` returns `max(viewW/fbW, viewH/fbH)` for RDP (`viewH = canvas.getRdpFullViewHeight() > 0 ? rdpFullViewHeight : canvas.getHeight()`) so no black borders appear at minimum zoom; non-RDP returns `canvas.getMinimumScale()` unchanged. The floor uses the PHYSICAL full-screen height (round 3) — not the shrunk `visibleHeight` — because the round-1/2 self-referential floor disabled `movePanToMakePointerVisible`'s vertical pan on devices where the window does not resize when the IME opens. The floor is recomputed on every call so it tracks the live `rdpFullViewHeight` (rotation / multi-window resize). Pinch-to-zoom clamp at `ZoomScaling.changeZoom:131-144` (round 5 — verified, no code change). Round 6: `FitToScreenScaling.changeZoom:91-141` is a new override that mirrors `ZoomScaling` with the floor fixed at the precomputed `minimumScale`; pinch-in now works in fit-to-screen mode (was a silent no-op pre-round-6). | `bVNC/src/main/java/com/iiordanov/bVNC/ZoomScaling.java:131-144, :223-235`, `bVNC/src/main/java/com/iiordanov/bVNC/FitToScreenScaling.java:91-141`, `RemoteCanvas.java:117, :911-920` |
| INV-018 | `InputAreaState` (RDP-only) is the single source of truth for the IME / "123" grid / hidden state of `@+id/rdpInputAreaContainer`. `RemoteCanvasActivity.setInputAreaState` is the only mutator; visibility is reapplied after the state is set, then `recomputeRdpViewport` is called (RDP-gated) so the viewport tracks the new container visibility. Anything that hides the IME on RDP must NOT collapse `EXTRA → NONE` — the grid must survive the IME hiding. Round 3: the `KEYBOARD ↔ NONE` transition is also driven by the IME insets listener (`onCreate:322-...`) on top of the legacy `relayoutViews` 19% heuristic. Round 5: the IME-hidden `onBackPressed:1808-1817` branch does NOT mutate `InputAreaState` — only `disconnectAndFinishActivity` is reachable from there. | `bVNC/src/main/java/com/iiordanov/bVNC/input/InputAreaState.java`, `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvasActivity.java:322-..., :343, :621, :1648-1664, :1661-1663, :1677-1708, :1793-1821` |
| INV-019 | All RDP-only UX elements are gated by `Utils.isRdp(this)`: `ModifierRowView`, `RdpExtraGridPanel`, `RdpModifierRowHandler`, `rdpInputAreaContainer`, the IME insets listener, `recomputeRdpViewport`, `RemoteCanvas.rdpFullViewHeight` accessors, RDP touchpad gestures (`TouchInputHandlerTouchpad.setRdp(true)`), round-5 `movePanToMakePointerVisible()` inside `Flinger.run:667`, round-5 "123" grid `MATCH_PARENT` insert in `RdpExtraGridPanel.init:93-95`, round-5 double-back disconnect in `onBackPressed:1808-1817`. **Round 9 additions to the RDP-only carve-out**: `PointerAccelerationCurve` (gated inside `TouchInputHandlerTouchpad.getDelta` by `isRdp`), the `rdpPointerAccel*` prefs (`rdpPointerAccelEnabled` / `rdpPointerAccelLowGainPct` / `rdpPointerAccelHighGainPct` / `rdpPointerAccelReset`) plus the `GlobalPreferencesFragment` reset action, the `MousePointerPosition` dedup in `RemoteRdpPointer.sendPointerEvent` (VNC/SPICE pointers untouched), and the `TouchInputHandlerTouchpad.onManualDoubleTap` override (touchpad-only; the base `onManualDoubleTap` runs on all flavors), plus round-10 precise finger tracking (`rdpPreciseTracking` + the `rdpPreciseFingerTracking` pref + `setPointerPreciseTracking`), and **round 12**: the `BUTTON_HEIGHT_TO_WIDTH_RATIO` (`1.5f`) and `CELL_MARGIN_DP` (`4f`) constants plus the `mRdpGridMode` field and `setRdpGridMode(boolean)` setter in `ExtraKeysView` (RDP-only — the legacy VNC/SPICE/Opaque 2-row pager stays at zero margin and aspect-ratio sizing), the post-layout `OnLayoutChangeListener` in `ExtraKeysView` (RDP-only; derives `cellWidth = (width − colCount × 2 × marginPx) / colCount` and overrides each button's height to `cellWidth × BUTTON_HEIGHT_TO_WIDTH_RATIO`, with a `lp.height == targetHeight` guard against re-entry), the gated per-cell margin in `ExtraKeysView.reload` (0 when `!mRdpGridMode`, 4 dp when `mRdpGridMode`), the `RdpExtraGridPanel` rewrite — now implements `ExtraKeysView.IExtraKeysView` and replaces the static `GRID_JSON` with `LEFT_SECTION_TEMPLATE` + `RIGHT_SECTION_SPECIAL` / `RIGHT_SECTION_NUMPAD` JSON fragments stitched by `rebuildGrid()` (toggle label flips between `"Number Keys"` and `"Special keys"`), with a local `NUMLK` intercept in `RdpExtraGridPanel.onExtraKeyButtonClick:191-204` that runs before the delegate (see INV-032), the `PRIMARY_REPETITIVE_KEYS` expansion from 8 to 22 entries (added F1–F12, ENTER, HOME, END, and arithmetic operators; `SYSRQ`/`NUMLK`/`INS` excluded), and the `recomputeRdpViewport` change in `RemoteCanvasActivity` to reserve `toggleReserved = toggleH + bottomMarginPx + 8 dp` only when `lastImeHeightPx == 0` AND `rdpInputAreaContainer` is `VISIBLE` AND `keyboardToggleButton` is `VISIBLE` (no reserved gap when the IME is open, since the toggle sits behind it), with `rdpInputAreaContainer.setTranslationY(-imeOverlap − toggleReserved)` repositioning the input area to clear the toggle. **Round-7 carve-out:** `keyboardToggleButton` and `toolbarToggleButton` are no longer RDP-only — both FABs are visible on every flavor. `keyboardToggleButton` click routes to `onKeyboardToggleButtonClicked:1757-1773` which still branches on `Utils.isRdp(this)` (RDP → `rdpModifierRowHandler.onBackToKeyboard()`, non-RDP → `showKeyboard()` / `hideKeyboard()`); `toolbarToggleButton` is flavor-agnostic. The RDP `InputAreaState` machine, IME insets listener, modifier row, "123" grid, pointer acceleration curve, and pointer dedup remain RDP-only as before. Non-RDP flavors (bVNC/aSPICE/Opaque) must remain byte-identical to the pre-round-9 UX for those subsystems. | `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvasActivity.java:300-302, :322-..., :1131-1174, :1415-..., :1648-1664, :1677-1708, :1757-1773, :1793-1821, :1839-1850, :1381-1397`, `bVNC/src/main/java/com/iiordanov/bVNC/GlobalPreferencesFragment.java:18-25, :54-69`, `bVNC/src/main/java/com/iiordanov/bVNC/extrakeys/RdpExtraGridPanel.java:93-95`, `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:138-..., :171-175, :421-455, :628-693`, `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpPointer.java:110-136` |
| INV-020 | (Round 3.) `RemoteCanvasActivity.recomputeRdpViewport` is the **single source of truth** for `canvas.setVisibleDesktopHeight` and `rdpInputAreaContainer.setTranslationY` on RDP. The legacy `relayoutViews` shrink block (`setVisibleDesktopHeight + relativePan`) is RDP-gated out (`:521-524`). `rdpFullViewHeight` is only ever set to the physical full-screen height (`canvas.getHeight()` when the IME is closed, or `canvas.getHeight() + lastImeHeightPx` if the IME was already open on the first capture) — never to the shrunk viewport. Any change to RDP viewport math must route through `recomputeRdpViewport`. | `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvasActivity.java:1677-1708`, `RemoteCanvas.java:911-920` |
| INV-021 | (Round 4, **amended round 9**.) The runtime tunables `edgeThresholdDp` (default 35 dp), `flingResistance` (default → damp 0.86), and `mouseAccelerationStrength` (default 1.0) reproduce the legacy hardcoded behavior byte-for-byte **on non-RDP flavors**. The `RemotePointer.accelerated` master boolean and the `setRelativeEvents` toggle path are unchanged by the slider wiring: `computeAcceleration` lerps `delta + (curve - delta) * strength`, gated by the same `accelerated` flag it always used. **Round 9 amend** (RDP only): pointer acceleration is now driven by `PointerAccelerationCurve`, a velocity-based gain curve (`V_REF_DP_PER_S = 500f`, EMA α = 0.4, gain = `gLo + (gHi - gLo) · v² / (v² + vRef²)`, clamped [0.05, 20]); defaults reproduce a Microsoft-RDP-style family (enabled, 1.0× slow, 3.0× fast) and intentionally **differ** from the legacy `computeAcceleration` curve. The legacy `mouseAccelerationStrength` slider is hidden in `GlobalPreferencesFragment` when `Utils.isRdp(getContext())`. Non-RDP flavors MUST remain byte-identical to the pre-round-9 UX for these subsystems. | `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:43-65, :171-175, :628-693`, `bVNC/src/main/java/com/iiordanov/bVNC/GlobalPreferencesFragment.java:18-25`, `remoteClientLib/src/main/java/com/undatech/opaque/input/RemotePointer.java:46-47`, `remoteClientLib/src/main/java/com/undatech/opaque/input/PointerAccelerationCurve.java:37-126` |
| INV-022 | (Round 4, **amended round 9**.) The RDP-only adaptive double-click+drag commits at a fixed density-independent threshold, not the platform touch slop: `rdpTouchSlop = max(2, DRAG_THRESHOLD_DP * displayDensity)` px, **`DRAG_THRESHOLD_DP = 8f` round 9** (was `2f` round 4; the 2 dp value was below typical finger jitter and converted intended double-clicks into drags). Round 2/3 used `getScaledTouchSlop() / 2` and was too coarse, cancelling genuine intended drags on tiny finger motion. | `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:55, :122, :494` |
| INV-023 | (Round 4.) `EdgePinRepeater` lifecycle is strictly scoped to a committed double-click+drag. Started only from `onTouchEvent` `ACTION_MOVE` while `rdpDoubleTapDragging == true` and the finger sits in the 24 dp `EDGE_PIN_BAND_DP` band at a canvas edge (driven by `updateEdgePinRepeater`). Stopped on `ACTION_UP`, `ACTION_CANCEL`, `ACTION_DOWN`, `setRdp(false)`, `onLongPress` (mutual exclusion with the synthesized right click), and when the finger leaves the band or the remote pointer is edge-clamped. The repeater must never clear `dragMode` / `rdpDoubleTapDragging` — ending the drag stays owned by `UP`/`CANCEL`/`setRdp(false)`. | `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:91, :125, :343, :391, :401, :416, :437, :593, :686-740` |
| INV-024 | (Round 6.) On-wire modifier VK release is exclusively a user-toggle-OFF side effect. ONLY `RdpModifierRowHandler.onRowModifierStateChanged:308-322` (which fires on a row toggle, not on a key dispatch or a disconnect) may call `RemoteRdpKeyboard.releaseOnScreenModifiers(int)` → `RdpCommunicator.releaseModifierKeys(int)`, and the release path is gated by `RemoteKeyboardState.isRemoteKeyDown(int)` so a bit that was never physically sent DOWN cannot be released. Key consumption (`consumeOnModifiers` on `KeyDispatchedListener`), `resetRowState` on disconnect, and `clearMetaState` must NOT call `releaseModifierKeys` — they only mutate `onScreenMetaState`; held-on-wire VKs are released via the round-5 dummy key-up sweep in `RemoteConnection.closeConnection`. The legacy VNC/SPICE pager (`RemoteExtraKeysHandler`) is not affected. | `bVNC/src/main/java/com/iiordanov/bVNC/extrakeys/RdpModifierRowHandler.java:308-322`, `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpKeyboard.java:143-152`, `remoteClientLib/src/main/java/com/undatech/opaque/RdpCommunicator.java:236-256`, `remoteClientLib/src/main/java/com/undatech/opaque/input/RemoteKeyboardState.java:220-232`, `remoteClientLib/src/main/java/com/undatech/opaque/input/RemoteKeyboard.java:202-208`. Covered by `RemoteKeyboardStateTest` (`remoteClientLib/src/test/java/com/undatech/opaque/input/RemoteKeyboardStateTest.java`). |
| INV-025 | (Round 6 + round-6-follow-up.) Predictive-back is opted out project-wide via `android:enableOnBackInvokedCallback="false"` on `<application>` in `bVNC/src/main/AndroidManifest.xml:29`. Inherited by every wrapper. The manifest flag is **necessary but not sufficient** for connected RDP sessions: it restores framework dispatch to `Activity.onBackPressed`, but the canvas View's `OnKeyListener` (`RemoteClientsInputListener.kt:60-62`) must also return `false` for `KEYCODE_BACK` on RDP (and TV), otherwise the listener forwards BACK to the remote as `VK_ESCAPE` (`RdpKeyboardMapper.keymapAndroid[KEYCODE_BACK] = VK_ESCAPE`, `RdpKeyboardMapper.java:343`) and returns `true`, consuming the press at the View level so `onBackPressed` never runs. The round-6-follow-up fix added the `isTv || Utils.isRdp(activity)` short-circuit; together with the manifest flag it makes round-5's double-back disconnect (`onBackPressed:1808-1817`) reachable on Android 13+. If `OnBackPressedDispatcher` support is added properly (per-flavor handlers in `RemoteCanvasActivity` / `ConnectionListActivity` / `GlobalPreferencesActivity`), the manifest flag MUST be removed — see ADR-0002. Until then, document both pieces as a deliberate, scoped opt-out. | `bVNC/src/main/AndroidManifest.xml:29`, `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvasActivity.java:1808-1817`, `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteClientsInputListener.kt:60-62`, `remoteClientLib/src/main/java/com/undatech/opaque/input/RdpKeyboardMapper.java:343`, `docs/DECISIONS/ADR-0002-enable-on-back-invoked-callback.md`. |
| INV-026 | (Round 8.) `RemoteSessionService` MUST be started in `RemoteCanvasActivity.onCreate` only after `connection.isReadyForConnection()` is true (`:426`), and MUST be stopped by `RemoteConnection.closeConnection` (`:315`) so every disconnect path (user-initiated `disconnectAndFinishActivity`, remote-initiated `RDP_CONNECT_FAILURE` / `RDP_UNABLE_TO_CONNECT` / `RDP_AUTH_FAILED`, error paths, re-init failures) is covered. The defensive `RemoteSessionService.stop(this)` in `RemoteCanvasActivity.onDestroy:1571` is a belt-and-braces fallback for the rare path where `closeConnection` was skipped — both call sites are idempotent. Notification ID MUST stay stable (`0x52445353`) so re-invocations update the existing slot rather than stacking; channel importance MUST stay `LOW` so re-emits do not beep; `PendingIntent` MUST use `FLAG_IMMUTABLE` (API 31+). On Android 13+ the start helper additionally requests `POST_NOTIFICATIONS` (`:1470-1478`) before issuing `RemoteSessionService.start(...)`; the service starts regardless of the prompt outcome (OS keeps the service alive without notification UI on deny) — see `features/FOREGROUND_SESSION_SERVICE.md` §4.1. See PAT-017. |
| INV-027 | (Round 9.) `RemoteRdpPointer.sendPointerEvent:130-136` emits at most one `MOUSE_BUTTON_MOVE`-flagged PDU per call. The `MOUSE_BUTTON_MOVE \| pointerMask` write is gated by `(pointerMask & MOUSE_BUTTON_MOVE) == 0`; when `pointerMask` already contains the MOVE bit (drag ticks `0x9800`, plain moves `0x0800`), only the bare `pointerMask` PDU is sent. Button-press sequences (`0x9800 → 0x9000`), release/scroll sequences, the INV-011 release-previous-button guard, and coordinates are unchanged. RDP-only — `RemoteVncPointer` / `RemoteSpicePointer` are untouched. The dedup halves the number of PDUs sent on drag ticks / plain moves (also halves `inputExecutor` time on moves). Residual risk: drag ticks still carry `DOWN`; if xrdp's grab still churns per-PDU, follow-up MOVE coalescing may be needed. Device verification pending. | `bVNC/src/main/java/com/iiordanov/bVNC/input/RemoteRdpPointer.java:110-136` |
| INV-028 | (Round 9.) The relaxed-slop double-tap detector is `DoubleTapPairTracker` (`remoteClientLib/src/main/java/com/undatech/opaque/input/DoubleTapPairTracker.java`), fed from raw primary-pointer `ACTION_DOWN`/`ACTION_UP` in `TouchInputHandlerGeneric.onTouchEvent:716-749`. Effective thresholds are framework-floored: `slopPx = max(doubleTapSlopDp × density, viewConfiguration.getScaledDoubleTapSlop())` and `timeoutMs = max(doubleTapTimeoutMs, viewConfiguration.getDoubleTapTimeout())`; defaults therefore behave like stock but the tracker adds detection for pairs stock rejected (a real-finger double-tap drifting > ~8 dp still emits a double-click). Round 9 deletes the round-3 buffered-`onSingleTapUp` detector (structurally unreachable — stock `GestureDetector` distance-rejects every pair that would have reached `onSingleTapUp`). `stockDoubleTapFired` is a per-dispatch flag set in `onDoubleTap` and checked after the detector feed — when true, the manual `onManualDoubleTap` hook is skipped so a stock-accepted pair is not double-emitted. `suppressNextSingleTapConfirmed` is set in `onManualDoubleTap` and cleared at the start of the next primary `ACTION_DOWN` (not in `onSingleTapUp` — that path would re-introduce the round-3 4-click bug). Tracker reset on `ACTION_CANCEL`, on `ACTION_POINTER_DOWN` (multi-touch transition), and from every subclass `onLongPress`. `TouchInputHandlerTouchpad.onManualDoubleTap` overrides the base hook to arm the adaptive state machine instead of the legacy two-click default. `DoubleTapPairTracker` is unit-covered by `DoubleTapPairTrackerTest` (14 tests, deterministic — caller supplies event times). | `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerGeneric.java:109-225, :440-500, :616-625, :716-749`, `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:373-376, :421-455`, `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerDirectDragPan.java:63-68`, `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerSingleHanded.java:162-167`, `remoteClientLib/src/main/java/com/undatech/opaque/input/DoubleTapPairTracker.java`, `remoteClientLib/src/test/java/com/undatech/opaque/input/DoubleTapPairTrackerTest.java`. |
| INV-029 | (Round 10.) RDP-only **precise finger tracking**: when `TouchInputHandlerTouchpad.rdpPreciseTracking` is true (setting `rdpPreciseFingerTracking`, default false), cursor movement is exactly `viewDelta / zoom` in every move/drag/fling/edge-pin path and touchpad sensitivity, `displayDensity`, `cbrt(zoom)`, and the `PointerAccelerationCurve` are all bypassed; sub-pixel carry is retained and `getDelta` is unchanged. `getPreciseZoom()` falls back to `1f` when `viewable.getZoomFactor() <= 0`. With the toggle OFF or on non-RDP sessions, the round-9 paths are byte-identical. `setPointerPreciseTracking(boolean)` resets the curve EMA + carry on every toggle and stops in-flight flinger / edge-pin repeaters when enabling. `GlobalPreferencesFragment.applyPreciseTrackingGreying` disables the four bypassed settings rows while the toggle is ON (the `rdpPointerAccelReset` action stays enabled). Device verification pending — no unit tests were added (`:bVNC` has no test source set). | `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java:50, :201-208, :225-228, :313-321, :653-657, :693-697, :879-884, :959-964`, `bVNC/src/main/java/com/iiordanov/bVNC/GlobalPreferencesFragment.java:90-107`, `bVNC/src/main/res/xml/global_preferences_rdp.xml:15-19`, `bVNC/src/main/java/com/iiordanov/bVNC/Constants.java:186, :215`, `bVNC/src/main/java/com/iiordanov/bVNC/RemoteCanvasActivity.java:967, :1326, :1389-1391` |

---

## PER — Performance rules

| ID | Rule | Why |
|---|---|---|
| PER-001 | Throttle between successive RDP input events: `sleepBetweenInputEvents` = 3ms (`RdpCommunicator.java:289-294`). Do not raise above 5ms without retesting for visible lag. Avoid dropping below 1ms — FreeRDP render queue saturates. | Empirically tuned for servers; values are persisted as constants. |
| PER-002 | Frame repaint throttle (~60 Hz → 10 Hz under load): `RemoteCanvas.reDraw` (`RemoteCanvas.java:681`). Bypassing it causes native bitmap-copy contention. | See INV-006. |
| PER-003 | Touch input handling is offloaded to a `Handler` from `RemoteClientsInputListener`. Do not block the input thread on network or DB I/O. | Visible jank. |
| PER-004 | No Compose, no animations beyond what `RemoteToolbar` and `appcompat` provide. Do not add `Animator` chains that mutate canvas-affecting state. | Every animation step tends to enter the input pipeline via invalidation. |
| PER-005 | `Bitmap.createBitmap(w, h, ARGB_8888)` is the only allocation path for the RDP framebuffer (per `UltraCompactBitmapData`). Resize only via `OnSettingsChanged` (server-driven). | Allocation cost dominates startup; one bitmap reuses across the session. |
| PER-006 | RDP-only touchpad fling emits up to ~50 move events/s via `TouchInputHandlerTouchpad.Flinger` (`FLING_TICK_MS=20`). Per-tick velocity decay is the runtime-configurable instance field `flingDamp` (default `FLING_DAMP=0.86f`, pushed from `RemoteCanvasActivity.getFlingResistanceDamp` via `setFlingDamp` — slider default 6 → `0.92f - slider*0.01f` = 0.86, the legacy value). The loop stops when either axis drops below `FLING_NOISE_PX_PER_S=200`. The flinger reuses `RdpCommunicator.inputExecutor`-equivalent sending for pointer events. The round-4 `EdgePinRepeater` reuses the same `FLING_TICK_MS=20` on `viewable.getHandler()` (main-thread UI handler) — keep them in lockstep if the tick ever changes. | Keeps fling on the same single-thread ordering as the rest of the input pipeline. Doubling the tick rate would double the load on `inputExecutor` with no perceptible gain. |

---

## PAT — Cross-references to pattern docs

See `PATTERNS.md` for the implementation idioms. Headlines:

- PAT-001 — `Protocol-per-flavor`: `RemoteRdpConnection` / `RemoteVncConnection` / `RemoteSpiceConnection` / `RemoteOvirtConnection` selected at runtime by `Utils.isRdp`.
- PAT-002 — `Modifier state lives in three places`: hardware / on-screen / sent-state. See `features/INPUT_PIPELINE.md`.
- PAT-003 — Single input executor per `RdpCommunicator`.
- PAT-004 — Sticky modifier UI synced to keyboard via the INV-010 bridge. Two callers (legacy pager + RDP modifier row).
- PAT-013 — RDP-only touchpad gestures (fling, long-press=right-click, **adaptive** double-tap=press-and-drag-or-double-click) gated by `setRdp(boolean)`. Round 4 added **drag-hold edge pinning** (`EdgePinRepeater`, INV-023) and made the double-tap-commit slop a fixed `DRAG_THRESHOLD_DP=2f` (INV-022). **Round 9 amend**: `DRAG_THRESHOLD_DP` is now `8f` (INV-022), the manual double-tap detector is `DoubleTapPairTracker` (INV-028) — the round-3 buffered-`onSingleTapUp` detector was structurally unreachable — and `onDoubleTap` delegates to `onManualDoubleTap` so a stock-accepted pair and a relaxed-slop pair both feed the same state machine.
- PAT-014 — RDP cover-scale zoom minimum (no black borders at minimum zoom; round 3 uses `rdpFullViewHeight`, not the shrunk `visibleHeight`). Round 6 added `FitToScreenScaling.changeZoom` so pinch-in works in fit-to-screen mode (INV-017).
- PAT-015 — RDP-only `InputAreaState` state machine for the IME / "123" grid UX; round 3 routes viewport math through `recomputeRdpViewport` and the IME insets listener. Round 5's double-back disconnect (INV-018 / INV-025) does NOT touch `InputAreaState`.
- PAT-016 — Runtime-configurable input tunables: a `SeekBarPreference` row → `Constants.<KEY>` + default → `RemoteCanvasActivity` getter → consumer instance field + setter, re-pushed from `onCreate` (edge only) + `onResume` (live re-apply on Settings return) + `getInputHandlerById` (freshly built handler picks up live slider). Defaults reproduce legacy hardcoded values (INV-021). Round 6 added the RDP-only modifier-row sizing sliders (`rdpModifierKeyHeightDp` / `rdpModifierKeySizeDp`) in `global_preferences_rdp.xml` — same shape, two push sites (`onResume` + `setInputHandler`) instead of three, since there is no lazily-built handler that needs a third push. **Round 9 amend** (RDP-only carve-out): three new `rdpPointerAccel*` prefs (enabled / lowGainPct / highGainPct) plus a `rdpPointerAccelReset` click action — same RDP-only overlay shape; the legacy `mouseAccelerationStrength` slider is hidden on RDP via `GlobalPreferencesFragment.onCreatePreferences`.
- PAT-017 — Foreground service for an in-flight long-running session: single canonical start (foreground context), single canonical stop (every disconnect path funnels through one teardown method), defensive idempotent second stop from `Activity.onDestroy`, stable notification ID + LOW channel importance + `FLAG_IMMUTABLE`. On Android 13+ the start helper additionally requests `POST_NOTIFICATIONS` (the service still runs without the notification UI on deny). See `features/FOREGROUND_SESSION_SERVICE.md`.
- INV-024 (modifier-release on toggle-OFF only) and INV-025 (predictive-back opt-out) are the round-6 invariants — see the INV table above and `features/INPUT_PIPELINE.md` §4.3.4 / §4.3.6 for the rationale and ADR-0002 for the back-press opt-out.
- INV-026 (round 8) is the foreground-service-lifecycle invariant — service starts only after `isReadyForConnection()` in `RemoteCanvasActivity.onCreate`, stops in `RemoteConnection.closeConnection` (single canonical close point); see PAT-017 and `features/FOREGROUND_SESSION_SERVICE.md`.
- INV-027 (round 9) is the single-PDU-per-pointer-event invariant for RDP — `RemoteRdpPointer.sendPointerEvent` skips the byte-identical `MOVE | pointerMask` write when `pointerMask` already carries the MOVE bit; see `features/INPUT_PIPELINE.md` §2.3 and `known_issues/fixed/BUG-004-click-drag-xrdp.md`.
- INV-028 (round 9) is the relaxed-slop double-tap detector invariant — `DoubleTapPairTracker` (raw DOWN/UP, framework-floored thresholds) replaces the structurally-unreachable buffered-`onSingleTapUp` approach; see `features/INPUT_PIPELINE.md` §2.2 and `known_issues/fixed/BUG-003-touchscreen-double-tap-slop.md` ("Follow-up 2026-09-12" section).
- INV-029 (round 10) is the RDP-only precise finger-tracking invariant — `TouchInputHandlerTouchpad.rdpPreciseTracking` applies gain exactly `1/zoom` in every move/drag/fling/edge-pin path and bypasses touchpad sensitivity, density and the acceleration curve; see `features/INPUT_PIPELINE.md` §2.2.1 / §6 and PAT-013 / PAT-016.

---

## Known gaps

These are real issues observed in the codebase; not yet ADRs. They are the most likely targets for upcoming refactors.

| Gap | Where | Why it matters |
|---|---|---|
| `compile.bat` and `app/build.gradle.kts` are referenced in `AGENTS.md` but do not exist on disk. | `AGENTS.md` §"Build & Test" | Documentation drift; treat AGENTS.md as illustrative for build scripts. |
| `RemoteConnection.handler` is static (GAP-INV-005). | `RemoteConnection.java` | Multi-instance RDP races. Workaround exists in `RdpCommunicator.patchFreeRdpCore` for `sessionMap` only. |
| `clearlAllModifiers()` unused (GAP-INV-009). | `RdpKeyboardMapper.java:664` | Modifier state can leak across reconnects if the user returns with a sticky modifier active. |
| Horizontal scroll wheel drops events silently (GAP-INV-012). | `RemoteRdpPointer.java:73,79` | Two-finger horizontal swipe → nothing on remote. |
| `GlobalApp.sessionMap` was `HashMap` upstream; synchronized via reflection patch. | `RdpCommunicator.patchFreeRdpCore:100-112` | Brittle — fix upstream instead. |
| `inputExecutor` and `DisconnectThread` are never explicitly shut down. | `RdpCommunicator.java:62, ~210` | Threads leak until process death (small leak; OK in practice). |
| Hardware-meta and remote-meta leak across reconnects within the same Activity instance. | `RemoteKeyboardState.detectHardwareMetaState`, `RemoteConnection.closeConnection:278-310` | RDP modifier-row clears its own state on disconnect (INV-008) but `hardwareMetaState` / `remoteKeyboardMetaState` still leak. |
| No unit tests for the new RDP-only view layer. | `bVNC/src/main/java/com/iiordanov/bVNC/extrakeys/ModifierRowView.java`, `RdpModifierRowHandler.java`, `RdpExtraGridPanel.java`, `bVNC/src/main/java/com/iiordanov/bVNC/input/TouchInputHandlerTouchpad.java` (RDP-only gestures, round-3/9 adaptive double-tap state machine, round-10 precise finger tracking), `bVNC/src/main/java/com/iiordanov/bVNC/input/InputAreaState.java`, `RemoteCanvasActivity.recomputeRdpViewport` | `:common` is the only module with JUnit 4 (see `TESTING.md` §1). Round 6 added `:remoteClientLib` JUnit coverage for `RemoteKeyboardState` (INV-024's `isRemoteKeyDown` gate). Round 9 extracted two more pure-logic peers: `DoubleTapPairTracker` (14 tests, INV-028) and `PointerAccelerationCurve` (11 tests, INV-021 amend). The rest of the RDP-only view layer is still manual-smoke-only — Android View classes resist pure-logic extraction. |
| Predictive-back is opted out project-wide. | `bVNC/src/main/AndroidManifest.xml:29` | Acceptable today (round-5 disconnect flow runs) but costs the predictive-back animation on `ConnectionListActivity` / `GlobalPreferencesActivity`. Migration to `OnBackPressedDispatcher` per Activity is the proper fix — tracked by ADR-0002. |
| AGENTS.md mentions `compile.bat` but it doesn't exist. | `AGENTS.md` table | Use `gradlew.bat assembleDebug` directly. |

---

## What "out of scope" means here

These are decisions not to relitigate in PRs:

- Migrating from Java Views to Compose.
- Adding `ViewModel` to RemoteCanvasActivity.
- Replacing the per-flavor wrapper-module structure with `productFlavors`.
- Replacing `AppCompatImageView` with a GL or `Surface` canvas.
- Direct edits inside the vendored `:remoteClientLib:...:freeRDPCore` C sources.
